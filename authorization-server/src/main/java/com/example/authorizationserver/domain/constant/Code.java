package com.example.authorizationserver.domain.constant;

public enum Code {
    NOT_READABLE,
    REQUEST_VALIDATION_ERROR,
    INVALID_SCOPE,
    USER_ALREADY_EXISTS,
    UNAUTHORIZED,
    INVALID_CREDENTIALS
}
